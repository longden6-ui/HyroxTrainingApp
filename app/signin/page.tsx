import { SigninForm } from '@/src/components/auth/SigninForm';

export const metadata = {
  title: 'Sign In - HYROX Coach AI',
  description: 'Sign in to your HYROX Coach AI account.',
};

export default function SigninPage() {
  return <SigninForm />;
}
