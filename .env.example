# Database
DATABASE_URL="postgresql://hyrox_user:hyrox_password@localhost:5432/hyrox_db"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email (password reset) — if SMTP_HOST is unset, reset links are logged to the
# server console instead of being emailed, so the flow still works locally.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="no-reply@hyroxcoach.ai"
